# Copy to config.env and fill in values from your ITUP console
# (username -> Copy login command -> the --server URL and your namespace).
#
#   cp config.env.example config.env
#
# config.env is gitignored — never commit real cluster details.

CLUSTER_API=https://<CLUSTER_API_SERVER>:6443
NAMESPACE=your-team--pipeline
